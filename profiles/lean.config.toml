# Optional reference profile for a deliberate manual opt-in; it is not installed by scripts/install.
approval_policy = "on-request"
sandbox_mode = "workspace-write"
model_reasoning_effort = "medium"

[agents]
enabled = false

[features]
hooks = true
goals = false
